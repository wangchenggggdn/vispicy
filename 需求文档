# AI生成图片与视频工具站需求文档

## 一、项目简介
开发一个基于AI的图片与视频生成工具站，支持文生图、图生图、文生视频、图片转视频等功能，面向创作者和普通用户，提供便捷的AI内容生成服务。网站部署在Vercel平台。

## 二、核心功能
1. 文生图（Text-to-Image）：输入文本生成图片，分辨率选择，图片下载。
2. 图生图（Image-to-Image）：上传图片+描述生成新图片。
3. 文生视频（Text-to-Video）：输入文本生成短视频，视频下载。
4. 图片转视频（Image/Images-to-Video）：上传图片生成动画/视频。
5. 用户管理与订单记录：google登录、记录用户购买订单。
6. 工具与扩展：图片/视频简单编辑工具，API接口开放。

## 三、技术选型
- 前端：Next.js（React）、Tailwind CSS/Ant Design、TypeScript
- 后端：Node.js（API路由）、Serverless Functions（Vercel原生支持）使用 SWR 或 React Query（推荐）
- AI服务：第三方AI模型API（如Stable Diffusion、Midjourney、OpenAI、Pika Labs等），或自建推理服务
- 存储：Vercel自带/第三方云存储（如Supabase、AWS S3）
- 数据库：Supabase（主数据库，负责用户、订单记录）
	> 当前数据库仅有模型表，需补充用户表和订单记录表，以支持用户管理和订单功能。
	> 用户表需包含：
	> - 用户金币数量字段
	> - 订阅类型字段（目前仅支持“周订阅”和“年订阅”）
- 鉴权：NextAuth.js/OAuth
- 部署：Vercel

## 四、页面与交互
- 首页：功能介绍、入口导航
- 文生图页、图生图页、文生视频页、图片转视频页：输入、参数选择、生成与结果展示
- 用户中心：登录、订单记录、金币余额、每日签到
- 订阅页：订阅选项、支付接口【暂时没有支付接口】

## 五、每日签到功能

### 5.1 功能概述
在用户中心页面添加每日签到功能，用户每天可以签到获取金币奖励，鼓励用户每日活跃。

### 5.2 奖励配置
- 7天签到奖励分别为：第1天20金币、第2天50金币、第3天100金币、第4天60金币、第5天80金币、第6天50金币、第7天120金币
- 签到可以间断（非连续签到），记录已签到的天数即可
- 7天全部签完后，重新从第1天开始循环

### 5.3 技术实现

#### 数据库表结构
**表名：** `daily_checkins`

| 字段 | 类型 | 说明 |
|------|------|------|
| id | UUID | 主键 |
| user_id | UUID | 用户ID（外键关联users表） |
| checkin_date | DATE | 签到日期 |
| day_number | INTEGER | 签到的天数（1-7） |
| coins_rewarded | INTEGER | 奖励的金币数量 |
| created_at | TIMESTAMP | 创建时间 |

**索引：**
- idx_daily_checkins_user_id：优化用户签到记录查询
- idx_daily_checkins_checkin_date：优化日期查询

#### 后端实现
**文件位置：** `lib/supabase.ts` (第528-620行)

**核心函数：**
- `getUserWeeklyCheckins(userId)` - 获取用户本周7天签到记录
- `getTodayCheckinStatus(userId)` - 检查用户今天是否已签到
- `performDailyCheckin(userId)` - 执行签到操作，自动发放金币到users.coins字段
- `getDailyRewards()` - 返回7天奖励配置数组 [20, 50, 100, 60, 80, 50, 120]

**签到逻辑：**
1. 检查今天是否已签到，若已签到则返回错误
2. 获取用户最近7天的签到记录
3. 找到第一个未签到的天数作为今天的签到天数
4. 如果7天都已签完，则从第1天重新开始
5. 创建签到记录并增加用户金币

#### API接口
**路径：** `/api/user/checkin`

**GET请求：** 获取签到状态和奖励配置
```json
{
  "hasCheckedToday": false,  // 今天是否已签到
  "checkedDays": [1, 2],      // 已签到的天数
  "nextDay": 3,               // 下一次可签到的天数
  "rewards": [20, 50, 100, 60, 80, 50, 120],  // 7天奖励配置
  "todayCheckin": null        // 今天的签到记录（如有）
}
```

**POST请求：** 执行签到
```json
{
  "success": true,
  "dayNumber": 3,             // 签到的天数
  "coinsReward": 100,         // 获得的金币奖励
  "checkin": { ... }          // 签到记录详情
}
```

#### 前端实现
**文件位置：** `app/(pages)/user/page.tsx`

**UI组件：**
- 签到卡片显示在用户中心页面，位于个人信息卡片和生成历史之间
- 显示7天签到进度网格，每个格子显示天数、奖励金币数
- 已签到：绿色背景，带✓标记
- 今日可签到：橙黄色渐变背景，带脉冲动画
- 未签到：白色背景，灰色文字

**交互逻辑：**
- 使用 SWR 获取签到状态数据
- 点击"Check In Now"按钮执行签到
- 签到成功后显示提示弹窗（如：🎉 Check-in successful! Day 3: +100 coins）
- 自动刷新签到状态和金币余额（通过 coins-update 事件）

**视觉设计：**
- 卡片背景：橙色到黄色渐变（from-orange-50 to-yellow-50）
- 签到按钮：橙黄色渐变，带阴影效果
- 已签到状态：绿色背景显示"✓ Checked in today!"
- 下一次奖励提示：显示"Next reward: +XXX coins"

### 5.4 相关文件清单
- 数据库迁移：`supabase/migrations/016_create_daily_checkins.sql`
- 后端函数：`lib/supabase.ts` (第528-620行)
- API接口：`app/api/user/checkin/route.ts`
- 前端页面：`app/(pages)/user/page.tsx`
  - 导入 Gift 图标（第7行）
  - 签到状态管理（第68-72行）
  - 签到处理函数（第74-102行）
  - 签到UI组件（第335-405行）